on:
  push:
    branches:
      - main

permissions:
  contents: write
  issues: write
  pull-requests: write

name: release-please

jobs:
  release-please:
    runs-on: ubuntu-latest
    outputs:
      desktop--release_created: ${{ steps.release.outputs['apps/desktop--release_created'] }}
      desktop--tag_name: ${{ steps.release.outputs['apps/desktop--tag_name'] }}
    steps:
      - uses: googleapis/release-please-action@8bb7a2ed0f90c9802c83129a9488d235a1f31a7c
        id: release
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          config-file: release-please-config.json
          manifest-file: .release-please-manifest.json
          skip-github-pull-request: ${{ startsWith(github.event.head_commit.message, 'chore: release') }}

  desktop-release:
    needs: release-please
    if: ${{ needs.release-please.outputs.desktop--release_created == 'true' }}
    uses: ./.github/workflows/release-desktop.yml
    with:
      tag_name: ${{ needs.release-please.outputs.desktop--tag_name }}
    secrets: inherit
